// Admin middleware placeholder
// Authentication and authorization checks are handled in the page component
// since role verification requires server API calls

export default () => {
    // No-op - auth handled in page
};
